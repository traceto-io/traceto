# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
*.egg

# Environment & secrets — NEVER commit these
.env
.env.local
.env.production
*.pem
*.key

# Database
*.db
*.sqlite
*.sqlite3

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test artefacts
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Logs
*.log

# CLI config (contains API key — never commit)
httrace.config.yaml
httrace.config.yml
.httrace.yaml

# Generated test output (belongs in customer repos, not here)
tests/integration/

# Stripe CLI
.stripe/

# Backend — NEVER push (lives only on the server)
backend/

# Infrastructure & session docs — NEVER push (contain server IPs, paths, secrets)
PROJECT_STATUS.md
IMPLEMENTATION_PLAN.md
SESSION_*.md
INFRA_*.md
*.infra.md

# Local-only directories
video/
demo/
legal/
sdk/

# Build artifacts
sdk-ruby/*.gem
node_modules/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
